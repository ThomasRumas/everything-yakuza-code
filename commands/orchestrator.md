---
description: "Run the Architect agent to produce a full implementation plan — business spec, BDD criteria, risk assessment, phased tasks, complexity estimate, and testing strategy. Waits for confirmation before any code is written."
argument-hint: "Feature or change to plan (e.g. 'add user authentication with OAuth')"
---

Use the `orchestrator` skill to plan the following: $input
