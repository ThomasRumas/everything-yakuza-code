---
description: "Archive the current .agents/spec.md into .agents/archived/ under a given name. Use when a feature is complete to preserve its plan, execution, refactoring, and testing reports."
argument-hint: "Archive name (e.g. 'user-auth', 'notifications')"
---

Use the `archive` skill to archive the current spec: $input
